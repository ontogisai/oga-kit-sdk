// Package ontology defines the OntologyRegistrar interface and types for
// registering entity types in the platform's semantic ontology layer.
// Registered types become discoverable by the Knowledge Agent via the schema
// cache and support semantic (vector) search.
package ontology
