// Package gateway provides the PlatformGatewayClient for accessing platform
// services from domain agent sidecars. All platform service access (MCP tools,
// LLM, workflows, inter-agent calls, registry) goes through a single endpoint:
// the Platform Access Gateway.
package gateway
